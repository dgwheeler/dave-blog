---
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { Image } from 'astro:assets';
import Comments from '../components/Comments.astro';
import { COMMENTS_ENABLED } from '../consts';

type Props = CollectionEntry<'blog'>['data'] & { slug: string };
const { title, description, pubDate, updatedDate, heroImage, slug } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} />
	</head>
	<body>
		<Header />
		<main>
			<article>
				{heroImage && <img class="hero" src={heroImage.src || heroImage} alt={title} />}
				<h1>{title}</h1>
				<p class="date"><FormattedDate date={pubDate} />{updatedDate && <span> · Updated <FormattedDate date={updatedDate} /></span>}</p>
				<hr />
				<slot />
				{COMMENTS_ENABLED && <Comments slug={slug} />}
			</article>
		</main>
		<Footer />
	</body>
</html>
